const apl = require('./template.json');

module.exports = (pixelCount, coinAmount, premium) => {

	const pixel = {
		"type": "Frame",
		"alignSelf": "center",
		"height": "2vw",
		"width": "2vw",
		"backgroundColor": "@myRed"

	};

	for (let i = 0; i < pixelCount; i++) {
		apl.
		
	}
}